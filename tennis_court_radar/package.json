{
  "name": "tennis-court-radar",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --format=cjs --outfile=dist/bundle.cjs --minify && esbuild src/frontend/app.tsx --bundle --platform=browser --format=iife --outfile=public/app.js --minify --jsx=automatic --jsx-import-source=preact",
    "dev": "tsc --watch",
    "start": "node dist/bundle.cjs"
  },
  "dependencies": {
    "@fastify/static": "^8.0.0",
    "cheerio": "^1.1.0",
    "fastify": "^5.0.0",
    "preact": "^10.29.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.27.3",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
